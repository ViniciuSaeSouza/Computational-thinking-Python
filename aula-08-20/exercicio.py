# EXERCÍCIOS
# 1. Escreva uma função que recebe uma lista de elementos e retorne a quantidade
# de elementos únicos (distintos) na lista.
# def elementos_unicos(ls:list) -> int:
#   return len(set(ls))
  
# lista_exercicio = [1, 2, 2, 3, 3, 3, 4, 4, 4, 4,]

# resp = elementos_unicos(lista_exercicio)

# print(resp)
 
 
# 2. Escreva uma função que recebe uma lista de elementos e retorne a quantidade
# de elementos duplicados na lista.

# def elementos_duplicados(ls:list) -> int:
#   return len(ls) - len(set(ls))

# lista_exercicio = [1, 2, 2, 3, 3, 3, 4, 4, 4, 4,]
# resp = elementos_duplicados(lista_exercicio)

# print(resp)
